plugins {
    id 'java-library'
}

subprojects {

    apply plugin: 'java-library'

    dependencies {
        api rootProject
    }

    jar {
        archiveBaseName.set("bukkit-tools-" + archiveBaseName.get())
    }
}

allprojects {
    println("hello from " + name)
}
