apply plugin: 'java'
apply plugin: 'maven-publish'

group 'clepto'
version '3.1.3'

compileJava.options.encoding 'UTF-8'
tasks.withType(JavaCompile) {
    options.encoding 'UTF-8'
}

repositories {
    mavenLocal()
    mavenCentral()
    maven {
        url 'https://repo.implario.dev/nms'
        credentials {
            username = 'nms'
            password = 'LIg7hj4G9iQIka1YnWnHi3eaPR7c07DlK2jboIMjnM7XJwCFagr2LMPHyZyEsnJI'
        }
        authentication {
            basic(BasicAuthentication)
        }
    }
    maven {
        url "https://repo.implario.dev/public"
    }
}

dependencies {
    testImplementation 'junit:junit:4.12'
    annotationProcessor 'org.projectlombok:lombok:1.18.12'
    compileOnly 'org.projectlombok:lombok:1.18.12'
    compileOnly 'com.destroystokyo.paper:paper:1.12.2-escalated'
    compileOnly 'clepto:clepto-java:0.3.9'
    compileOnly 'org.codehaus.groovy:groovy:3.0.5'
}

jar {
    from sourceSets.main.resources
    from {
        duplicatesStrategy = DuplicatesStrategy.EXCLUDE
        configurations.runtimeClasspath.collect { it.isDirectory() ? it : zipTree(it) }
    }
}

publishing {
    repositories {
        maven {
            def args = publishMavenUri.split ' '
            url = args[0]
            credentials {
                username = args[1]
                password = args[2]
            }
            authentication {
                basic(BasicAuthentication)
            }
        }
    }
    publications {
        maven(MavenPublication) {
            groupId = rootProject.group
            artifactId = rootProject.name
            version = rootProject.version
            from components.java
        }
    }
}